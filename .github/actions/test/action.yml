name: 'Test'
description: 'Test'
runs:
    using: 'composite'
    steps:
        - name: Install dependencies
          uses: ./.github/actions/yarn-install
          with:
              clean-cache: 'true'

        - name: Run tests
          shell: bash
          run: yarn test
